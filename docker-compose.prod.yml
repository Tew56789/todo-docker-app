version: "3.9"

services:
  backend:
    image: tew56789/todo-backend
    ports:
      - "5000:5000"

  frontend:
    image: tew56789/todo-frontend
    ports:
      - "80:80"
    depends_on:
      - backend
